# Night window 2026-08-28: #50897 validated cross-arch, Scout-20GB screens equal-and-faster

**Window:** 2026-08-28 00:10–~02:00 CEST, both boxes (prod down, restored after).
**Three jobs, in Tim's priority order — all three landed.**

## 1. vllm#50897 ported and validated (prefix cache × speculation)

The candidate fix for the 0%-prefix-cache-hits-under-spec gap (the RTX
adoption gate, [board #38 context](gridbook-nvfp4-dflash2-rtx-triad.md)) was
ported onto both serving lineages:

- **sm120**: full merge onto the v4 integration base
  (`TechPrototyper/vllm@integrate/dflash2-nvfp4-v4-pc50897`). 15 conflicts:
  13 mechanical (parameter threading vs. the newer `num_prefill_lookahead`
  plumbing), the Mooncake connector family taken as HEAD (inert here, *not
  upstreamable as-is*), and **one semantic seam**: `KVCacheCoordinator.
  cache_blocks` now carries finalized-KV truncation for multi-module MTP;
  resolved as *hashing on → the PR's full aligned caching, hashing off →
  truncation unchanged* — an empirical question the determinism gate then
  answered.
- **sm121**: core-only 3-way apply of the PR's semantic diff (18 files,
  conflict-free) onto the older experiment lineage
  (`vllm-dflash2-v2exp@exp/pc50897`).

**Exact-replay probe** (12,812-token deterministic prompt, greedy
temp0/seed0, DFlash2 drafter, NVFP4 KV, server-side hit counters —
[`results/RESULT_pc50897_replay_probe.json`](../results/RESULT_pc50897_replay_probe.json)):

| arm | replay cache hits | replay vs cold output |
|---|---:|---|
| sm120 unpatched (2cf8b8ae0) | **0 / 12,812** | byte-identical |
| sm120 + #50897 | **11,648 / 12,812 (90.9%)** | **byte-identical** |
| sm121 + #50897 | **11,648 / 12,812 (90.9%)** | **byte-identical** |

*(2026-08-28 morning: validation posted upstream —
[50897#issuecomment-5448682111](https://github.com/vllm-project/vllm/pull/50897#issuecomment-5448682111).)*

Both architectures land on the same hit count **to the token**; the
remainder is boundary alignment by design. Byte-identity of cold vs
replayed completions is the load-bearing result: reused cache does not
touch greedy output — including across the port's semantic seam, and under
a *diffusion-style* drafter outside the MTP/EAGLE family the PR targets
(sm121 additionally ran the fp8 compressed-tensors drafter).

**Battery, patched arms** (n=250 GSM screening, cache active): sm120
0.976, det 5/5, 77.5 tok/s single / 176.2 c=8; sm121 0.972, needle 6/6,
det 5/5, 22.0 / 51.2. No cache-attributable anomaly. *No paired quality
delta claimed* — the matched no-PR reference for this exact config doesn't
exist; byte-identity is the stronger statement anyway.

## 2. vllm#52883: the fix is obsolete at its site — and needed one file over

Analysis + CPU probe
([`probes/lmhead_quant_method_guard_probe.py`](../probes/lmhead_quant_method_guard_probe.py)):
the guard jschmied fixes was removed by the merged #52816 refactor (his
crash scenario now passes — verified), but the **same
`UnquantizedEmbeddingMethod`-vs-`UnquantizedLinearMethod` confusion
survives** in `LogitsProcessor._apply_head`'s `head_dtype` branch, which
falsely rejects exactly his scenario (verified: B OK / C RAISED, controls
A/D OK). Retarget recommendation + re-validation offer posted:
[52883#issuecomment-5446093771](https://github.com/vllm-project/vllm/pull/52883#issuecomment-5446093771).

## 3. Scout-20GB head-to-head: statistically equal, ~10% faster

`rdtand/Qwen3.8-27B-PrismaScout-AQUA-20GB` (19.97 GB verified) vs our
serving AQUA-5.5 (23.63 GB), **identical config both arms** (v4 image,
NVFP4 KV pinned 3 GiB, 32k, no spec, greedy):

| | Scout-20GB | AQUA-5.5 |
|---|---:|---:|
| GSM8K n=250 | **0.9720** | **0.9720** |
| discordant items | 2 | 2 |
| McNemar exact | p = 1.0 | |
| repeat determinism | 5/5 | 5/5 |
| tok/s single | **74.3** | 67.4 |
| tok/s c=8 | **272.7** | 247.5 |

Boot smoke: stock compressed-tensors, serves on the unmodified v4 image.
The speed gap tracks the 3.66 GB weight-read difference (consistent with
rdtand's own Spark measurement). **Max KV pool at prod shape** (0.97 util,
262k, NVFP4 KV, no spec): Scout **477,569** vs AQUA-5.5 **342,604** tokens
(captured at prod restore, same shape) — **+134,965 tokens, +39.4% KV
pool** on the 32 GB card, purely from the smaller weight footprint.
Adoption stays gated on the **n=1319 verdict** (separate window) per
method discipline.

## Failures & artifacts of the night (all recoverable, all documented)

- Downloader OOM-killed at 4 Gi (snapshot_download buffering) → 12 Gi + 2 workers.
- PVC hit 100% mid-download → pruned 11.7 G of ≥14-day-old torch-compile
  caches (AOT reuse note respected: recent entries kept).
- First baseline serve OOM'd at first request → the §18 config details
  (cudagraph capture sizes 1/2/4) are load-bearing, now baked into the specs.
- Spark arm boot #1: missing `/fi` mount (flashinfer import) → fixed.
- Two wrong-merge-base attempts on the Spark port (stale FETCH_HEAD,
  jschmied's base SHA confused with ZJY0516's) → pinned refs, true base.
- Battery `tools: 0/6` on all night arms is a serve-config artifact
  (tool-parser flags not set in the night pods), not a regression.

**Restore:** RTX `vllm-qwen38` scaled back + Flux resumed (verified
un-suspended and reconciled); Spark battery (vllm-aura38 + 4
embedder/reranker) restarted; both serves health-checked green by ~01:52
CEST. AQUA prod KV pool at restore: 342,604 tokens @ 0.97/262k.
